spring:
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  messages:
    basename: validation

  datasource:
    url: jdbc:postgresql://localhost/schedule
    username: dbadmin
    password: hihanpan
    sql-script-encoding: UTF-8

  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp

